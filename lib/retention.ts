/**
 * 保存期間。イベントは最終更新からこの日数で自動削除される。
 *
 * 他人の名前とコメントを預かる公開ツールなので、無期限には持たない。
 * 日程調整は数週間で終わるので、90日あれば実用上困らない。
 *
 * この値は cron の掃除（custom-worker.ts）と /privacy の文面の両方が参照する。
 * 片方だけ変えると、書いてあることと実際の挙動がずれる。
 */
export const RETENTION_DAYS = 90;
